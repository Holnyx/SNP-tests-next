import React, { memo } from 'react';
import AdminPage from '@/components/pages/AdminPage/AdminPage';

const TakeTests = () => {
  return <AdminPage admin="admin"></AdminPage>;
};

export default memo(TakeTests);
